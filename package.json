{
  "name": "wunschlachen-crm",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx pnpm@latest --filter app dev",
    "build": "npx pnpm@latest --filter app build",
    "generate": "npx pnpm@latest --filter app generate && mkdir -p .output && cp -r app/.output/public .output/public",
    "preview": "npx pnpm@latest --filter app preview",
    "postinstall": "npx pnpm@latest --filter app postinstall"
  },
  "devDependencies": {
    "nuxt": "^3.6.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
